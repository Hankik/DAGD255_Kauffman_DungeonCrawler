class NPC extends Actor {

  // fields
  
  NPC(float x, float y, float w, float h){
    
    name = "npc";
  
    this.x = x;
    this.y = y;
    setSize(w, h);
  }
  
  void update(){}
  
  void draw(){}
}
